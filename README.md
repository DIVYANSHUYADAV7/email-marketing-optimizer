# Email Marketing Optimizer

Simple, reproducible pipeline that:
- Generates sample contact data
- Trains two models to predict open & click probability
- Scores contacts and ranks them
- Produces personalized email templates (A/B variants)

## Quickstart

1. Create a Python virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate    # macOS / Linux
   .venv\Scripts\activate       # Windows PowerShell
